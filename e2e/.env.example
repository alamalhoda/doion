# Copy to e2e/.env for local overrides (gitignored via root .env patterns if named .env).

FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:8000/api/v1

# Must match backend seed_demo --password (local/demo only).
DEMO_SEED_PASSWORD=password123

# Absolute path to checkyar-googleai clone (optional; used by docs/scripts notes).
# CHECKYAR_UI_PATH=/Users/you/Projects/checkyar-googleai
