server {
    listen 80;
    server_tokens off;

    location / {
        include /etc/nginx/proxy_params;
        proxy_pass http://0.0.0.0:3000;
    }

    location /static/ {
        alias /app/staticfiles/;
    }

    location /media/ {
        alias /app/doion/media/;
    }
}