---
description: Keep TODO/README/PLAN and project docs synchronized after major changes and before every PR
globs:
  - "backend/**/*.py"
  - "frontend/src/**/*.{js,ts,vue}"
  - "shared/**/*.{js,ts}"
  - "docs/**/*.md"
  - "**/*README*.md"
  - "**/*TODO*.md"
  - "**/*PLAN*.md"
alwaysApply: false
---

# Documentation Sync Policy

## Goal

بعد از تغییرات مهم (به‌خصوص قبل از ساخت PR)، مستندات پروژه باید با وضعیت واقعی کد همگام شوند.

## Mandatory Checklist (Before PR)

1. اثر تغییر را تعیین کن: frontend / backend / shared / API / process.
2. فایل‌های مستنداتی مرتبط را بازبینی و در صورت نیاز به‌روز کن.
3. اگر تغییری لازم نبود، دلیل "عدم نیاز به تغییر مستندات" را در توضیح PR ذکر کن.

## Minimum Docs Coverage

- تغییرات cross-domain یا اولویت‌ها -> `TODO.md`
- تغییرات backend -> `backend/TODO.md`, `backend/README.md`
- تغییرات UI فعال (فعلاً repo خارجی) -> سیاست در `docs/development/FRONTEND_DEVELOPMENT_STATUS.md`؛ قرارداد API همچنان فقط در این مونورپو
- تغییرات UI داخل مونورپو (پس از مهاجرت به `frontend/`) -> `frontend/README.md` در صورت وجود
- تغییرات قرارداد API -> **`docs/development/MASTER_API_CONTRACT.md` فقط** (SSOT واحد؛ `API_CONTRACT_REGISTRY.md` deprecated stub است)
- تغییرات وضعیت flow صفحات -> `docs/development/PAGE_REVIEW_LOG.md`

## API Contract Rule (Mandatory)

هر تغییر در `serializers` / `views` / `urls` / `api_router` / `exception_handler` / model choices مرتبط با API باید **همان PR** را با به‌روزرسانی `docs/development/MASTER_API_CONTRACT.md` همراه کند.

**بدون sync مستند، تغییر API کامل نیست.**

- رفتار را از کد استخراج کن؛ حدس نزن.
- اگر چیزی فقط در spec است و در کد نیست، با برچسب `spec-only / not implemented` علامت بزن یا حذف کن.
- سند موازی جدید برای قرارداد API نساز.

## PR Rule

- هر PR باید یکی از این دو حالت را شفاف داشته باشد:
  - `Docs updated` (فایل‌های به‌روز شده ذکر شوند)
  - `Docs impact: none` (به‌همراه دلیل روشن)

## Guardrails

- وضعیت انجام کار را حدسی یا optimistic ثبت نکن؛ فقط بر اساس شواهد کد/تست.
- اگر تغییر behavior یا contract رخ داده، به‌روزرسانی doc را به PR بعدی موکول نکن.
- از ایجاد TODO تکراری در چند فایل پرهیز کن؛ برای هر موضوع، مرجع اصلی مشخص باشد.
